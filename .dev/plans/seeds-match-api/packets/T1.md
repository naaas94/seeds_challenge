# Executor Packet — T1: Project Scaffold
**Plan:** seeds-match-api v1.0 | **Skill:** executor-subtask-execution

---

## §1. Task Statement

Build a Python FastAPI application exposing a conversational AI agent backed by an explicit ReAct loop and a Yahoo Finance tool, with per-conversation in-process memory and two HTTP endpoints (`POST /chat`, `GET /chat/{conversation_id}`). The application runs in Docker via a single-worker uvicorn process. The primary evaluation signal is the explicit `agent_loop` function in `app/agent/loop.py`.

**Non-goals:** Persistence across restarts, authentication, rate limiting, streaming, multiple tool providers, multi-worker deployment, any LangChain `AgentExecutor` usage.

---

## §2. Shared Contracts (binding on all subtasks)

### Naming — Module Paths (binding for this subtask)

All directories and files must be created exactly:
```
app/__init__.py
app/agent/__init__.py
app/memory/__init__.py
tests/__init__.py
requirements.txt
.env.example
```

### CLI Surface (frozen)

| Consumer | String |
|----------|--------|
| Dockerfile CMD | `uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| Endpoint POST | `/chat` |
| Endpoint GET | `/chat/{conversation_id}` |

### Tests

Framework: pytest + FastAPI TestClient. Location: `tests/test_app.py`. No live API calls.

---

## §4. Subtask Spec

**ID:** T1  
**Scope:** Create the complete directory skeleton and all non-Python infrastructure files. No Python implementation logic in this subtask.

**Files to touch:**
- `requirements.txt`
- `.env.example`
- `app/__init__.py` (empty)
- `app/agent/__init__.py` (empty)
- `app/memory/__init__.py` (empty)
- `tests/__init__.py` (empty)

**Contract bindings:** Naming (module paths), CLI surface (requirements.txt package list)

**Inputs:** None

**Outputs:** Scaffold with all `__init__.py` files (empty), `requirements.txt`, `.env.example`

**Kill criteria:**
- HALT if `pydantic-settings` is absent from requirements.txt — pydantic BaseSettings import will fail.
- HALT if the umbrella `langchain` package is included — spec explicitly prohibits it (only `langchain-core` and `langchain-openai` are allowed).
- HALT if `app/agent/__init__.py` or `app/memory/__init__.py` are missing — Python package imports fail.

**Log tier:** `trivial`

**Risks & mitigations:** Risk: wrong package names. Mitigation: use exact names below.

---

## Exact File Contents

### `requirements.txt`
```
fastapi
uvicorn[standard]
langchain-core
langchain-openai
pydantic-settings
yfinance
```

### `.env.example`
```
# Copy to .env and fill in your key before running docker compose up.
OPENAI_API_KEY=sk-...
MODEL_NAME=gpt-4o-mini
```

### All `__init__.py` files
Empty files — zero bytes. Their presence makes the directories Python packages.

---

## §5. Filtered Adversarial Items

### Load-bearing assumptions (affecting T1)

None — T1 has no external dependencies and no contract surfaces that downstream subtasks consume, except the module path structure itself.

### Hidden couplings (affecting T1)

None confirmed. T2–T6 all depend on the module paths created here; if any path differs from spec §3, all downstream imports fail. The file list above is the exact authority.

---

## Resolved Inputs

No prior subtask outputs required. This is the root node.
