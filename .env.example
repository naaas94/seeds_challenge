# Copy to .env for local overrides (optional). Do not commit .env — see .gitignore.
#
# OPENAI_API_KEY: set in Windows (User/System environment variables), not in this file.
# Open a new terminal after setting it so `docker compose` and uvicorn see the value.
#
MODEL_NAME=gpt-4o-mini
